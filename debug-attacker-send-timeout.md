[OPEN] Debug Session: attacker-send-timeout

Symptom:
- Атакующий агент не может отправить данные на сервер (таймаут при POST /analyze).
- Polling /last-correlation возвращает 404.

Environment:
- Windows, localhost (127.0.0.1), порт 8443.

Hypotheses:
- Сервер на 8443 не запущен или слушает другой порт/протокол (HTTP vs HTTPS).
- Сервер запущен, но обработчик /analyze зависает или выполняется слишком долго (deadlock/блокировка).
- Клиент шлёт на неверный путь (/analyze) или сервер ожидает другой путь (получается 404/не тот хендлер).
- Проблема с TLS/сертификатом: клиент использует http:// на https-порту или наоборот, что приводит к таймауту/срыву соединения.
- Логи сервера пишутся в бинарной/UTF-16 кодировке, из-за чего мы не видим критичных ошибок; реальная причина есть в server.log.

Evidence:
- attacker.log: polling /last-correlation => 404, POST /analyze => timed out.

Next Steps:
- Проверить доступность 127.0.0.1:8443 и какие URL/эндпойнты реально есть.
- Декодировать logs/server.log и найти записи по /analyze.
- После подтверждения причины — минимальный фикс и повторная проверка.
